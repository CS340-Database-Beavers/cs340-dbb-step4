<table class={{page}} id="datatable">
  <thead>
    <tr>
      {{#each tableData.[0]}}
        <th>{{@key}}</th>
      {{/each}}
        <th>Remove</th>
    </tr>
  </thead>
  <tbody>

    {{#each tableData}}
      <tr id={{@index}}>
        {{#each this}}
          <td class={{@key}}>{{this}}</td>
        {{/each}}
          <td align="center" style="padding: 0px" class="removeicon">&#128465</td>
      </tr>
    {{/each}}
  </tbody>
</table>