<form class={{page}} id="dataform">
  <h2>{{title}}</h2>
  {{#each inputData}}
    <label for={{Field}}>{{Field}}:</label>
    <input type="text" id={{Field}} name={{Field}} class="{{Type}} {{Null}} {{Key}} datainput" />
  {{/each}}
  <input id="submit" type="submit" value="Submit" />
</form>